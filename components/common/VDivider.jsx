import { Divider } from '@chakra-ui/react';
import React from 'react';

const VDivider = ({styles}) => {
	return <Divider borderColor={`#979797`} my={`15px`} {...styles}/>;
};

export default VDivider;
